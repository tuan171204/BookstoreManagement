@model IEnumerable<BookstoreManagement.Models.Supplier>

@{
    ViewData["Title"] = "Quản lý Nhà cung cấp";
    Layout = "~/Views/Shared/_Layout.cshtml";
    
    // Helper function để tạo sortable header link
    Func<string, string, string, string> GetSortUrl = (column, currentSortBy, currentSortOrder) =>
    {
        var queryParams = new List<string>();
        
        if (ViewData["CurrentFilter"] != null && !string.IsNullOrEmpty(ViewData["CurrentFilter"].ToString()))
        {
            queryParams.Add($"searchString={Uri.EscapeDataString(ViewData["CurrentFilter"].ToString())}");
        }
        
        if (ViewData["IsActiveFilter"] != null && !string.IsNullOrEmpty(ViewData["IsActiveFilter"].ToString()))
        {
            queryParams.Add($"isActive={ViewData["IsActiveFilter"]}");
        }
        
        string newSortOrder = "asc";
        if (currentSortBy == column && currentSortOrder == "asc")
        {
            newSortOrder = "desc";
        }
        
        queryParams.Add($"sortBy={column}");
        queryParams.Add($"sortOrder={newSortOrder}");
        queryParams.Add($"pageNumber=1");
        
        return "?" + string.Join("&", queryParams);
    };
    
    string currentSortBy = ViewData["SortBy"]?.ToString() ?? "CreatedAt";
    string currentSortOrder = ViewData["SortOrder"]?.ToString() ?? "desc";
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Quản lý Nhà cung cấp</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item active">Nhà cung cấp</li>
    </ol>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="card mb-4">
        <div class="card-header">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <i class="fas fa-truck me-1"></i>
                    Danh sách Nhà cung cấp
                </div>
                <a asp-action="Create" class="btn btn-primary btn-sm">
                    <i class="fas fa-plus"></i> Thêm mới
                </a>
            </div>
        </div>

        <div class="card mb-4">
            <div class="card-body">
                <form asp-action="Index" method="get" class="row g-3">
                    <input type="hidden" name="pageNumber" value="1" />
                    <input type="hidden" name="sortBy" value="@currentSortBy" />
                    <input type="hidden" name="sortOrder" value="@currentSortOrder" />
                    <div class="col-md-6">
                        <label class="form-label">Tìm kiếm</label>
                        <input type="text" name="searchString" class="form-control"
                            placeholder="Tên, liên hệ, địa chỉ..." value="@ViewData["CurrentFilter"]" />
                    </div>
                    <div class="col-md-3">
                        <label class="form-label">Trạng thái</label>
                        <select name="isActive" class="form-select">
                            <option value="">-- Tất cả --</option>
                            <option value="true" selected="@(ViewBag.IsActiveParam == true)">Hoạt động</option>
                            <option value="false" selected="@(ViewBag.IsActiveParam == false)">Không hoạt động</option>
                        </select>
                    </div>
                    <div class="col-md-3 d-flex align-items-end">
                        <button type="submit" class="btn btn-primary w-100">
                            <i class="bi bi-search"></i> Tìm
                        </button>
                    </div>
                </form>
            </div>
        </div>
        <div class="card-body">
            @if (ViewBag.TotalItems > 0)
            {
                <div class="mb-3">
                    <span class="text-muted">Hiển thị <strong>@((ViewBag.PageNumber - 1) * ViewBag.PageSize + 1)</strong> - <strong>@(Math.Min(ViewBag.PageNumber * ViewBag.PageSize, ViewBag.TotalItems))</strong> trong tổng số <strong>@ViewBag.TotalItems</strong> nhà cung cấp</span>
                </div>
            }
            <div class="table-responsive">
                <table class="table table-striped table-bordered table-hover">
                    <thead class="table-light">
                        <tr>
                            <th>
                                <a href="@GetSortUrl("name", currentSortBy, currentSortOrder)" class="text-decoration-none text-dark">
                                    Tên Nhà Cung Cấp
                                    @if (currentSortBy == "name")
                                    {
                                        <i class="bi bi-arrow-@(currentSortOrder == "asc" ? "up" : "down")"></i>
                                    }
                                    else
                                    {
                                        <i class="bi bi-arrow-down-up text-muted" style="opacity: 0.3;"></i>
                                    }
                                </a>
                            </th>
                            <th>
                                <a href="@GetSortUrl("contactinfo", currentSortBy, currentSortOrder)" class="text-decoration-none text-dark">
                                    Liên hệ (SĐT/Email)
                                    @if (currentSortBy == "contactinfo")
                                    {
                                        <i class="bi bi-arrow-@(currentSortOrder == "asc" ? "up" : "down")"></i>
                                    }
                                    else
                                    {
                                        <i class="bi bi-arrow-down-up text-muted" style="opacity: 0.3;"></i>
                                    }
                                </a>
                            </th>
                            <th>
                                <a href="@GetSortUrl("address", currentSortBy, currentSortOrder)" class="text-decoration-none text-dark">
                                    Địa chỉ
                                    @if (currentSortBy == "address")
                                    {
                                        <i class="bi bi-arrow-@(currentSortOrder == "asc" ? "up" : "down")"></i>
                                    }
                                    else
                                    {
                                        <i class="bi bi-arrow-down-up text-muted" style="opacity: 0.3;"></i>
                                    }
                                </a>
                            </th>
                            <th>
                                <a href="@GetSortUrl("isactive", currentSortBy, currentSortOrder)" class="text-decoration-none text-dark">
                                    Trạng thái
                                    @if (currentSortBy == "isactive")
                                    {
                                        <i class="bi bi-arrow-@(currentSortOrder == "asc" ? "up" : "down")"></i>
                                    }
                                    else
                                    {
                                        <i class="bi bi-arrow-down-up text-muted" style="opacity: 0.3;"></i>
                                    }
                                </a>
                            </th>
                            <th class="text-center" style="width: 150px;">Thao tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td class="fw-bold">@item.Name</td>
                                    <td>@(item.ContactInfo ?? "-")</td>
                                    <td>@(item.Address ?? "-")</td>
                                    <td>
                                        @if (item.IsActive)
                                        {
                                            <span class="badge bg-success">Hoạt động</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">Không hoạt động</span>
                                        }
                                    </td>
                                    <td class="text-center">
                                        <div class="btn-group btn-group-sm">
                                            <a asp-action="Details" asp-route-id="@item.SupplierId"
                                                class="btn btn-info text-white" title="Chi tiết">
                                                <i class="bi bi-eye"></i>
                                            </a>
                                            <a asp-action="Edit" asp-route-id="@item.SupplierId" class="btn btn-warning"
                                                title="Sửa">
                                                <i class="bi bi-pencil-square"></i>
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@item.SupplierId" class="btn btn-danger"
                                                title="Xóa" onclick="return confirm('Bạn có chắc muốn xóa nhà cung cấp này?');">
                                                <i class="bi bi-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="5" class="text-center text-muted py-4">
                                    <i class="bi bi-inbox" style="font-size: 3rem; color: #ccc;"></i>
                                    <p class="mt-2">Không tìm thấy nhà cung cấp nào</p>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <!-- Pagination -->
            @await Html.PartialAsync("_Pagination")
        </div>
    </div>
</div>
